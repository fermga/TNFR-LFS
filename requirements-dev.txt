# Scientific stack required by the toolkit and the integration tests.
# Keep the version ranges aligned with ``requirements.txt``.
numpy>=1.24,<2.0
pandas>=1.5,<3.0

# Tooling for development workflows.
pytest>=7.0
ruff>=0.6.0
mypy>=1.11.0
black>=24.4.0
