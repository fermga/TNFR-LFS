# Scientific stack required by the toolkit and the integration tests.
# Keep the version ranges aligned with ``requirements.txt``; ``tomli`` entries must remain identical across both files.
numpy>=1.24,<2.0
pandas>=1.5,<3.0
tomli>=2.0.1; python_version < "3.11"

# Tooling for development workflows.
pytest>=7.0
pytest-asyncio>=0.21
ruff>=0.6.0
mypy>=1.11.0
black>=24.4.0
mkdocs>=1.5.0
mkdocs-static-i18n>=1.2.0
mkdocs-macros-plugin>=1.0.5
